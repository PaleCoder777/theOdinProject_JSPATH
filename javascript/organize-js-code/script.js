// File Connection Verification
let test = ("The JS page is conncted");
console.log(test);

// Organize Code with Objects Lesson

    /* TOP Overview Lesson
    const myObject = {
        property: "Value!",
        otherProperty: 77,
        "obnoxious property": function() {
            console.log("Function called, I am obnoxious function");
        },
    };


    const car = {
        make: "Volkswagen",
        model: "Golf",
        year: 2026,
        color: "blue",
        priceUSD: 40000,
        // a method is just a function assigned to a property
        applyDiscount: function(discountPercentage) {
            const multiplier = 1 - discountPercentage / 100;
            this.priceUSD *= multiplier;
            console.log(this.priceUSD);
        },
        // shorthand way to add method to object literal
        getSummary() {
            return `${this.year} ${this.make} ${this.model} in ${this.color}, priced at $${this.priceUSD} (USD).`;
        },
    };
    */

// Object Constructors

    // Lesson
